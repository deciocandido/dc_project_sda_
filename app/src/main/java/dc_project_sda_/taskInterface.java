package dc_project_sda_;

import java.util.Date;

public interface taskInterface {
    void taskTitle(String taskString);
    void taskProject(String taskProject);
    void taskStatus(Boolean taskStatus);
    void taskDueDate(Date taskDate);
}
